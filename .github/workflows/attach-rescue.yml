name: Attach MK Debt runtime rescue
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  attach:
    if: "!contains(github.event.head_commit.message, '[rescue]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Attach rescue runtime
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          tag='<script src="runtime-rescue.js"></script>'
          if tag not in s:
              s=s.replace('</body>',tag+'</body>',1)
              p.write_text(s,encoding='utf-8')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Attach runtime rescue [rescue]'
          git push origin HEAD:main