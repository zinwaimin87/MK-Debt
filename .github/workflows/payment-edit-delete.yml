name: Payment edit delete
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: legacy payment-edit-delete patch conflicts with the current MK Debt app."
